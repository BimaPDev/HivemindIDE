# The editor fork is a SIBLING repository (../hivemindide-editor) — see
# fork/README.md. If it is cloned inside this repo instead, keep it out of this
# repo's history; the fork scripts find it in either place.
/hivemindide-editor/

# Build output
dist/
*.exe

# Local overrides
.env
.env.local

.DS_Store
